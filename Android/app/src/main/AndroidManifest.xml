<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.exia.puydufou"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="22" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="fr.exia.puydufou.ViewManager.HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="fr.exia.puydufou.ViewManager.ActivitiesActivity"
            android:label="@string/activite" />
        <activity
            android:name="fr.exia.puydufou.ViewManager.PlanningActivity"
            android:label="@string/planning" />
        <activity
            android:name="fr.exia.puydufou.ViewManager.ParcoursActivity"
            android:label="@string/parcours" />
    </application>

</manifest>
